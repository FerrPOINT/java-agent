name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test (JDK ${{ matrix.java }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: ['25']
    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ matrix.java }}

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('backend/**/gradle-wrapper.properties', 'backend/**/*.gradle', 'backend/**/gradle.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew backend/gradlew

      - name: Run backend tests
        working-directory: backend
        run: ./gradlew test --no-daemon

      - name: Run backend slow integration tests
        working-directory: backend
        run: ./gradlew slowTest --no-daemon

      - name: Run telegram-bot and cli tests
        run: ./gradlew :telegram-bot:test :cli:test --no-daemon

      - name: Upload slow test reports
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: slow-test-reports-jdk${{ matrix.java }}
          path: backend/build/reports/tests/slowTest/

      - name: Upload test reports
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: test-reports-jdk${{ matrix.java }}
          path: |
            backend/build/reports/tests/test/
            telegram-bot/build/reports/tests/test/
            cli/build/reports/tests/test/

  build-image:
    name: Build Docker image
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '25'

      - name: Build bootJar
        working-directory: backend
        run: ./gradlew bootJar --no-daemon -x test

      - name: Build Docker image
        run: docker build -f Dockerfile -t java-agent:latest .

      - name: Docker Compose E2E test
        run: ./scripts/e2e-docker-compose-test.sh

      - name: Build slim Docker image
        run: docker build -f Dockerfile.slim -t java-agent:slim .

      - name: Smoke test slim image
        run: |
          docker run -d --name agent-smoke -p 8092:8080 \
            -e SPRING_PROFILES_ACTIVE=noop \
            -e AGENT_CHROMIUM_AUTO_START=false \
            -e AGENT_BROWSER_CDP_URL=http://localhost:9222 \
            -e AGENT_MEMORY_ENABLED=false \
            -e AGENT_SKILLS_ENABLED=false \
            java-agent:slim
          for i in $(seq 1 30); do
            curl -fsS http://localhost:8092/actuator/health/liveness && break
            sleep 2
          done
          docker stop agent-smoke
          docker rm agent-smoke

      - name: Upload Docker Compose E2E reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: e2e-docker-logs
          path: |
            /tmp/e2e-docker-compose.log
            backend/build/reports/tests/slowTest/

  lint:
    name: Markdown / YAML lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: articulate/actions-markdownlint@v1
        with:
          config: .markdownlint.json
          files: 'docs/**/*.md, *.md'
        continue-on-error: false
